#message
	- flash.each do |type, message|
		%div{class: "alert #{bootstrap_class_for(type)} fade in"}
			%button.close{"data-dismiss" => "alert"} ×
			.text-center
				= message
	- if @submission.errors.any?
		#error_explanation
			%h2= "#{pluralize(@submission.errors.count, "error")} prohibited this submission from being saved:"
			%ul
				- @submission.errors.full_messages.each do |msg|
					%li= msg
#form
	.jumbotron.text-center
		%h1 Satyaprit Das Memorial Scholarship
	.container
		.row
			.col-xs-12.col-sm-8.col-md-6.col-lg-6
				%span.header
					%h3 To the Graduating Class of 2014
					%h5 View/download the essay prompt <a href="https://s3-us-west-2.amazonaws.com/satyaprit-assets/Satyaprit+Scholarship+Prompt.docx">by clicking here</a>.
				= render 'form'
